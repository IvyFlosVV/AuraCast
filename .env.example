# Copy this file to .env and fill in your key. Do not commit .env (it is in .gitignore).
GEMINI_API_KEY=your_google_gemini_api_key_here

# Use Flash for higher free-tier limits (default)
GEMINI_MODEL=gemini-1.5-flash

# Free tier: if you hit "rate limit" often, try:
# - Wait 1–2 minutes between generating episodes.
# - Generate one episode at a time (avoid rapid repeated clicks).
# - Use fewer chars per request: GEMINI_MAX_INPUT_CHARS=15000
# - Wait longer before auto-retry: RETRY_DELAY_SEC=60

# Optional overrides
# GEMINI_MAX_INPUT_CHARS=30000
# RETRY_DELAY_SEC=30
# MAX_CONTENT_LENGTH=52428800
# MAX_TEXT_LENGTH=300000
# GEMINI_TIMEOUT=120
